# Parser Site — полный чек-лист (от начала до редизайна)

> Цель: вести проект по понятным этапам, с критериями готовности и контрольными артефактами.

## 0) Инициализация и база (Foundation)

- [x] Создан базовый Python-проект (`pyproject.toml`, пакетная структура, `README`).
- [x] Настроены базовые зависимости и тестовый раннер (`pytest`).
- [x] Добавлен `.gitignore` для служебных файлов.
- [x] Добавлен минимальный рабочий функционал парсинга.

**Критерий завершения:** проект устанавливается и базовые тесты проходят локально.

---

## 1) Домен парсинга v1 (MVP Parsing)

- [x] Извлечение email.
- [x] Извлечение URL (http/https).
- [x] Извлечение телефонов.
- [x] Нормализация результатов (email lower-case, очистка URL от хвостовой пунктуации, нормализация телефонов).
- [x] Удаление дублей с сохранением порядка появления.

**Критерий завершения:** входные тексты разных форматов дают ожидаемый структурированный результат.

---

## 2) CLI и UX использования (Developer Experience)

- [x] CLI принимает текст аргументом.
- [x] CLI умеет читать из файла (`--file`).
- [x] CLI умеет читать из stdin.
- [x] JSON-вывод + pretty режим (`--pretty`).
- [x] Добавить флаги селективного вывода (например, только `emails` / `urls` / `phones`).
- [x] Добавить коды возврата и человеко-понятные сообщения об ошибках ввода/файла.

**Критерий завершения:** CLI удобен в скриптах и ручном запуске, поведение документировано.

---

## 3) Качество и надежность (Quality Gate)

- [x] Базовые unit-тесты на текущую логику.
- [ ] Расширить edge-case тесты:
  - [x] URL со скобками/параметрами/пунктуацией в конце.
  - [x] Email с нестандартными, но валидными паттернами.
  - [x] Телефоны в разных локалях и форматах.
  - [x] Смешанные шумные тексты.
- [x] Добавить property-based / fuzz тесты для устойчивости regex-подхода.
- [x] Проверка производительности на больших текстах.

**Критерий завершения:** покрыты основные и пограничные сценарии, нет регрессий.

---

## 4) Архитектура v2 (Extensibility)

- [ ] Разделить слои: извлечение, нормализация, пост-обработка, сериализация.
- [ ] Добавить конфигурируемый pipeline (включение/отключение отдельных экстракторов).
- [ ] Подготовить интерфейс для подключения новых сущностей (например, соцсети, мессенджеры, адреса).
- [ ] Ввести typed API для настройки (dataclass / pydantic-модель конфигурации).

**Критерий завершения:** новые типы сущностей добавляются без переписывания ядра.

---

## 5) API/Интеграции (если нужно как сервис)

- [x] Поднять HTTP API (FastAPI) с endpoint `/parse`.
- [x] Валидация входных payload.
- [x] OpenAPI-спека и примеры запросов/ответов.
- [x] Добавить ограничение размера входа и timeout-политику.
- [x] Dockerfile + запуск в контейнере.

**Критерий завершения:** сервис разворачивается и стабильно обрабатывает внешние запросы.

---

## 6) Наблюдаемость и эксплуатация (Ops)

- [ ] Структурированные логи.
- [ ] Метрики (время парсинга, размер входа, доля ошибок).
- [ ] Health/readiness endpoint (для сервисного режима).
- [ ] CI пайплайн: lint + test + build.

**Критерий завершения:** проект готов к поддержке в прод-среде.

---

## 7) Редизайн (UI/UX Redesign, если делаем фронт)

- [ ] Определить целевой пользовательский сценарий (кто и как использует результат парсинга).
- [ ] Создать UX-flow: ввод текста → предпросмотр → валидация → экспорт.
- [ ] Подготовить дизайн-систему (цвета, типографика, компоненты).
- [ ] Сделать новый интерфейс:
  - [ ] drag-and-drop файла
  - [ ] подсветка найденных сущностей в тексте
  - [ ] панель фильтрации результатов
  - [ ] экспорт JSON/CSV
- [ ] Добавить e2e UI-тесты ключевых сценариев.
- [ ] Провести финальный accessibility-pass (контраст, клавиатурная навигация, ARIA).

**Критерий завершения:** новый UI быстрее и понятнее решает задачу парсинга, чем текущий поток.

---

## 8) Финальный релизный чек (Definition of Done)

- [ ] Документация актуальна (README + примеры).
- [ ] CHANGELOG обновлен.
- [ ] Версия повышена (semver).
- [ ] Все тесты зеленые в CI.
- [ ] Подготовлен релизный тег.

---

## Предложенный порядок «что делаем дальше прямо сейчас»

1. Перейти к **п.6 (Ops)**: структурированные логи, метрики и health/readiness политику.
2. Поднять CI пайплайн (lint + test + build).
3. После этого вернуться к **п.4** для архитектурного рефакторинга без регресса.

